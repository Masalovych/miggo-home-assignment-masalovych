# Frontend Developer Technical Home Assignment


## Install dependences
Run `npm install` in the `ui` and `api` folders.


## Run API

`cd api && npx ts-node src/index.ts`

## Run UI

`cd ui && npm run dev`

